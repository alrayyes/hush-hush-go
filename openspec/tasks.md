## 1. Repo setup

- [x] 1.1 Create `github.com/alrayyes/hush-hush-go`, public, from a suitable scaffold (repo-creation skill), and verify CI runs on the initial empty commit
- [x] 1.2 Add hush-hush's `api/` as a git submodule pinned to its current commit, and verify `git submodule status` shows a clean pin
- [x] 1.3 Set up `go.mod` targeting Go 1.26, and verify `go build ./...` succeeds on a bare module

## 2. Codegen pipeline

- [x] 2.1 Configure `oapi-codegen` to generate types and a client from the submodule's `openapi.yaml` into `client.gen.go`, and verify the file carries the `// Code generated by oapi-codegen; DO NOT EDIT.` header
- [x] 2.2 Wire generation into `go generate`, and verify running it twice against the same submodule commit produces a byte-identical `client.gen.go`
- [x] 2.3 Add a CI check that fails if `client.gen.go` differs from what regenerating from the pinned submodule commit produces, and verify it fails on a manual edit to that file

## 3. Hand-written client layer

- [x] 3.1 Implement the constructor (base URL, credential with `HUSH_HUSH_API_KEY` env fallback, timeout, max retries) and verify both scenarios in specs/sdk-go/spec.md's "Client construction" requirement pass as unit tests
- [x] 3.2 Implement the `X-Caller` functional option and verify it's attached per-request, not client-wide
- [x] 3.3 Implement retry/backoff (network failures, 5xx, 429 only; honors `Retry-After`) and verify the three scenarios in the "Retry behavior" requirement pass
- [x] 3.4 Implement typed `APIError` (status, parsed body, optional request ID) and verify the "Typed error mapping" requirement's scenario passes
- [x] 3.5 Implement the typed audit-log query method (filters, flat slice result — no iterator, hush-hush's `/audit-log` has no pagination) and the `GetObjectUsedBy` method, and verify the "Audit log query" and "Typed resource operations" requirements' scenarios pass

## 4. Testing

- [x] 4.1 Add unit tests for the hand-written layer only (3.1-3.5) against a fake transport, and verify `go test ./...` passes with no network calls
- [x] 4.2 Stand up a Prism mock server from the pinned spec in CI and run the full client surface against it, and verify contract tests pass
- [x] 4.3 Add a Pact consumer test recording the client's real interactions, and verify a pact file is produced and published as a CI artifact
- [x] 4.4 Add a thin end-to-end smoke test against a staging hush-hush instance, gated to run on release/nightly only, and verify it's excluded from the default PR pipeline

## 5. CI and release

- [x] 5.1 Add a scheduled/webhook-triggered regeneration workflow that opens a PR with the result, and verify it never pushes directly to the default branch
- [x] 5.2 Add an `openapi-diff` step classifying each regeneration PR's underlying spec change as additive or breaking, and verify the classification is visible on the PR
- [x] 5.3 Configure auto-merge for the initial scaffolding PR and for all regeneration PRs on green CI (full auto-merge grant for this session), and verify the `openapi-diff` classification is still surfaced on every PR for after-the-fact visibility even though it doesn't gate merge
- [x] 5.4 Wire semver bump decisions to the `openapi-diff` classification rather than Conventional Commits, and verify a test release computes the expected bump for both an additive and a breaking sample diff

## 6. Docs

- [x] 6.1 Add godoc comments to all exported identifiers in the hand-written layer, and verify `go vet` and `golint`-equivalent checks report no missing-doc warnings
- [x] 6.2 Write the README quickstart (install, auth setup, one real call) by hand, and verify a fresh reader can follow it to a successful first call against a local Prism mock

## 7. Cross-repo wiring

- [x] 7.1 Once the repo has real content and passes CI, request hush-hush's README be updated to link to it (coordinate with whoever owns that repo), and verify the link is live — alrayyes/hush-hush#83, merged by Ryan 2026-08-29, link confirmed live in the README's SDKs section
- [x] 7.2 Close hush-hush issue #74, linking to the new repo, and verify the issue is closed with that link in its final comment
